<UserControl x:Class="TournamentManager.Client.Views.WindowControls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="120">
    <StackPanel Orientation="Horizontal">
        <Button x:Name="MinimizeButton" 
                Style="{StaticResource SystemButtonStyle}"
                Click="MinimizeButton_Click">
            <materialDesign:PackIcon Kind="WindowMinimize" Foreground="White" Width="16" Height="16"/>
        </Button>

        <Button x:Name="MaximizeRestoreButton" 
                Style="{StaticResource SystemButtonStyle}"
                Click="MaximizeRestoreButton_Click">
            <materialDesign:PackIcon x:Name="MaximizeRestoreIcon" Kind="WindowMaximize" Foreground="White" Width="16" Height="16"/>
        </Button>

        <Button x:Name="CloseButton" 
                Style="{StaticResource SystemButtonStyle}"
                Background="{StaticResource ErrorBrush}"
                Click="CloseButton_Click">
            <materialDesign:PackIcon Kind="Close" Foreground="White" Width="16" Height="16"/>
        </Button>
    </StackPanel>
</UserControl>