<UserControl x:Class="TournamentManager.Client.Views.TournamentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:TournamentManager.Client.Converters"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:TournamentManager.Client.Views">

    <Grid Margin="16" Background="#FFF5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource WhiteBrush}" Padding="16" Margin="0,0,0,16" BorderThickness="0,0,0,1" BorderBrush="{StaticResource BorderBrush}">
            <StackPanel Orientation="Horizontal">
                <Button Content="Создать турнир"
                        Command="{Binding CreateTournamentCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Margin="0,0,12,0"/>

                <ComboBox ItemsSource="{Binding StatusFilters}"
                          SelectedItem="{Binding SelectedStatusFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          Width="160"
                          Margin="0,0,12,0"
                          Style="{StaticResource ComboBoxStyle}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Style="{StaticResource BodyTextStyle}" Padding="4"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                
                <StackPanel Orientation="Horizontal" Margin="0,0,12,0">
                    <TextBox Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Width="200"
                             Margin="0,0,4,0"
                             Tag="Найти"
                             Style="{StaticResource WatermarkTextBoxStyle}"/>
                </StackPanel>

                <Button Content="Сбросить все фильтры"
                        Command="{Binding ResetFiltersCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        ToolTip="Сбросить фильтры и обновить список"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="4">
            <ItemsControl ItemsSource="{Binding Tournaments}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="16"
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   VerticalAlignment="Center"
                                                   Margin="0,5,0,20"/>

                                        <Border Background="{Binding ., Converter={converters:TournamentStatusToColorConverter}}"
                                                CornerRadius="4"
                                                Padding="8,4"
                                                Margin="12,5,0,20"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="{Binding ., Converter={converters:TournamentStatusToStringConverter}}"
                                                       Foreground="White"
                                                       FontSize="11"
                                                       FontWeight="Medium"
                                                       HorizontalAlignment="Center"/>
                                        </Border>
                                    </StackPanel>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Margin="0,0,24,0">
                                            <TextBlock Text="Даты проведения"
                                                       FontSize="10"
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       Margin="0,0,0,2"/>
                                            <TextBlock Text="{Binding StartDate, StringFormat='Начало: {0:dd.MM.yyyy}'}"
                                                       FontSize="12"
                                                       Foreground="{StaticResource TextPrimaryBrush}"
                                                       FontWeight="Medium"/>
                                            <TextBlock Text="{Binding EndDate, StringFormat='Конец:   {0:dd.MM.yyyy}'}"
                                                       FontSize="12"
                                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" Margin="0,0,24,0">
                                            <TextBlock Text="Место проведения"
                                                       FontSize="10"
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       Margin="0,0,0,2"/>
                                            <TextBlock Text="{Binding Address}"
                                                       FontSize="12"
                                                       FontWeight="Medium"
                                                       TextWrapping="Wrap"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2" Margin="0,0,24,0">
                                            <TextBlock Text="Организатор"
                                                       FontSize="10"
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       Margin="0,0,0,2"/>
                                            <TextBlock Text="Федерация Тхэквондо"
                                                       FontSize="12"
                                                       FontWeight="Medium"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>

                                <StackPanel Grid.Column="1"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Top">
                                    <Button Command="{Binding DataContext.ViewTournamentDetailsCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource IconButtonStyle}">
                                        <materialDesign:PackIcon Kind="Eye"/>
                                    </Button>
                                    <Button Command="{Binding DataContext.EditTournamentCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource IconButtonStyle}">
                                        <materialDesign:PackIcon Kind="Pencil"/>
                                    </Button>
                                    <Button Command="{Binding DataContext.DeleteTournamentCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource IconButtonStyle}"
                                            Foreground="{StaticResource ErrorBrush}">
                                        <materialDesign:PackIcon Kind="Delete"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="1"
                Background="Transparent"
                Visibility="{Binding Tournaments.Count, Converter={converters:EmptyToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="🏆"
                           Foreground="{StaticResource GoldenBrush}"
                           FontSize="120"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,16"/>
                <TextBlock Text="Турниры не найдены"
                           FontSize="18"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>
                <TextBlock Text="Создайте первый турнир или измените параметры фильтрации"
                           FontSize="13"
                           Foreground="{StaticResource TextHintBrush}"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Background="{StaticResource ProgressBarBrush}"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True"
                             Width="200"
                             Height="8"
                             Margin="0,0,0,16"/>
                <TextBlock Text="Загрузка турниров..."
                           Foreground="{StaticResource TextPrimaryBrush}"
                           FontSize="14"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>